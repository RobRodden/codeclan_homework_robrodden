"0","movies_selected %>% 
  select(title, runtime, budget) %>% 
  group_by(title, runtime, budget) %>% 
  summarise(movie_selected, count = is.na(NA)) %>% 
  arrange(desc(count))
"
"1","[38;5;232m`summarise()` has grouped output by 'title', 'runtime', 'budget'. You can override using the `.groups` argument.[39m
"
